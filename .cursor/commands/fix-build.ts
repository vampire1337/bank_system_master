/**
 * Исправь ошибки, пока build не проходит
 */
export default `

Запусти команду \`npm run build\`.  
Если возникают ошибки компиляции (TypeScript, ESLint), устрани их.  
Повторяй процесс, пока \`next build\` не пройдёт успешно.

Предоставь reasoning и покажи исправления.

`;
